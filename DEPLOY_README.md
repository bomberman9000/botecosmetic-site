# 🚀 Деплой VIA LABOTE

## ✅ Что настроено:

1. **SSH алиас `bote`** — настроен в `~/.ssh/config`
   - Хост: `144.31.64.130`
   - Пользователь: `root`
   - Ключ: `~/.ssh/id_ed25519_bote_new`
   - ✅ Работает без пароля

2. **Скрипт деплоя** — `./DEPLOY_ONE_COMMAND.sh`
   - Использует алиас `bote`
   - Все команды с таймаутами
   - Подробные логи на каждом шаге
   - Healthcheck в конце

## 📋 Использование:

```bash
cd bote-site
./DEPLOY_ONE_COMMAND.sh
```

## 🔍 Что делает скрипт:

1. **STEP 1: Connect** — проверка подключения (timeout 10s)
2. **STEP 2: Build (local)** — сборка проекта локально (timeout 600s)
3. **STEP 3: Create archive** — создание архива для деплоя
4. **STEP 4: Upload** — загрузка на сервер (timeout 120s)
5. **STEP 5: Extract and install** — установка на сервере (timeout 300s)
6. **STEP 6: Restart PM2** — перезапуск приложения (timeout 60s)
7. **STEP 7: Healthcheck** — проверка статуса (timeout 10s)

## ✨ Особенности:

- ✅ Все команды с таймаутами (нет зависаний)
- ✅ Подробные логи на каждом шаге
- ✅ Автоматическая проверка статуса в конце
- ✅ Работает без пароля (через SSH ключ)

## 🌐 После деплоя:

Сайт доступен: http://144.31.64.130:3001
