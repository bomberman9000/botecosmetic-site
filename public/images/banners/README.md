# 📸 Banner Assets Guide

## Структура папок

```
public/images/banners/
├── promo-personal-desktop.png  (Персональный уход - Desktop)
├── promo-shop-desktop.png      (Магазин косметики - Desktop)
└── README.md                   (этот файл)
```

## Размеры экспорта (рекомендуемые)

### Desktop
- **Размер:** 1200×800px
- **Формат:** PNG (webp при оптимизации)
- **Focus point:** 
  - Персональный уход: `45% 65%` (продукты слева/внизу)
  - Магазин косметики: `70% 35%` (лицо справа)

### Tablet (опционально)
- **Размер:** 1280×720px
- **Используется:** автоматически масштабируется из desktop

### Mobile (опционально)
- **Размер:** 1080×1350px (вертикальный формат)
- **Используется:** автоматически масштабируется из desktop

## Правила экспорта

1. ✅ **Никакого текста на изображении** - текст добавляется в HTML
2. ✅ **Чистый фон** - беж/молочный, лёгкая текстура
3. ✅ **Тёплый свет** - без грязных затемнений
4. ✅ **Focus point** - важные элементы (продукты/лицо) не на краях

## Настройка object-position

В `app/page.tsx` можно настроить точку фокуса для каждого баннера:

```tsx
style={{ objectPosition: '45% 65%' }}  // Горизонталь % Вертикаль %
```

- `0% 0%` = левый верхний угол
- `50% 50%` = центр
- `100% 100%` = правый нижний угол

## Добавление нового баннера

1. Экспортируйте изображение в нужный размер
2. Сохраните в `public/images/banners/promo-{name}-desktop.png`
3. Обновите `app/page.tsx` с новой карточкой
4. Настройте `objectPosition` для правильного кропа

## CSS классы

- `.promo-card` - основной контейнер карточки
- `.promo-overlay` - градиент overlay (меняется на мобильных)
- `.promo-content` - блок с текстом
- `.promo-title` - заголовок (адаптивный размер)
- `.promo-kicker` - верхний текст ("Твой", "Готовый")
- `.promo-link` - ссылка внизу ("Подробнее →", "Перейти →")

## Адаптивность

- **Desktop (>1024px):** 2 колонки, высота 420px
- **Tablet (768-1024px):** 1 колонка, высота 380px
- **Mobile (<480px):** 1 колонка, высота 520px, вертикальный градиент

---

**Важно:** Все изображения оптимизируются автоматически через Next.js Image component.
